{
  "name": "woof-frontend",
  "version": "2.0.0",
  "description": "Woof - Instagram for Animals (Frontend)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && aws s3 sync dist/ s3://woof-app-frontend-2026/ --delete --exclude '.DS_Store'",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.9.3",
    "vite": "^6.0.6"
  },
  "author": "Tommi Kivisaari",
  "license": "UNLICENSED"
}
